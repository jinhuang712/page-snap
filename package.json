{
  "name": "lossless-web-scanner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "python3 -m json.tool manifest.json >/dev/null && node --check popup.js && node --check runner.js && node --check content-snapshot.js && node --check zip-store.js && node --test tests/zip-smoke.test.mjs && git diff --check"
  }
}
